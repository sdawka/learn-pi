# docs

Architectural source of truth for the learn-pi language-learning agent.

Start with [plan.md](./plan.md).

Sibling directories in the repo root:
- `learn-pi/` — the pi-mono package (extensions, skills, prompt templates, themes)
- `LearnVault/` — the git-synced markdown + SQLite state vault
